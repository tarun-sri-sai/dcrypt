import React from "react";
import { fontSizes, paddings } from "../utils/styles";

const Editor = () => {
  return <div className={`editor ${paddings.main} ${fontSizes.label}`}>Editor</div>;
};

export default Editor;
